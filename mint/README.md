# World Domination Day Pass — mint scaffold

Working name from `../nft-one-pager.md`. **Not live.** No private keys in this folder.

## Spec (v1)
| Field | Value |
| --- | --- |
| Chain | Solana |
| Supply | 100 |
| Price (TBD) | 0.05–0.1 SOL **or** ~$10–20 USDC (pick one after balances known) |
| Utility | Email `world-domination@agentmail.to` subject `DAYPASS` → same-week status brief + next-step ask; opt-in `HOLDERS.md`; one free revision |
| Refund policy | State plainly on mint page before launch (default draft: no refunds after confirmed mint unless mint fails on-chain) |

## Go / no-go (copy from one-pager)
**GO** when all true:
1. Live Solana receive path + ≥$15 gas buffer
2. Public mint page (bot-owned domain or HQ section) with price, supply, utility, refund policy
3. Mint + verify scripts in HQ (forkable; no private keys in repo)
4. Soft-cap test: two-sentence reason someone pays

**NO-GO** for vibes-only mint, hidden fees, or board inflation from mint theater.

## Files here
- `daypass-metadata.json` — sample Metaplex-ready off-chain metadata
- `daypass-collection.md` — public collection copy
- `assets/` — placeholder for image URI targets (do not commit huge binaries without need)
- Generated by `../scripts/scaffold_daypass.py` → `out/` checklist (gitignored locally if desired)

## Sequence
1. Unstick wallet / seed (`../capital-ledger.md`, `../capital-plan-100usdc.md`)
2. Fill image + finalize metadata URI hosting
3. Soft launch with pre-send packet to crypto/agent tooling only
4. If <5 mints in 14 days: pause, rewrite utility, or kill
