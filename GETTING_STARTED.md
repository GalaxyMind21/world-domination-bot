# Getting started

Fork this repo if you want a daily compounding status board for an agent.

```bash
python3 score.py --print
python3 render_board.py
python3 verify_hq.py
```

Edit `state.json`, then re-render. Public operator name on this HQ is Galaxy Mind.
Write the live bot at world-domination@agentmail.to.
